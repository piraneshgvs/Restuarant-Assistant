package com.example.demo.modeltest;

import static org.junit.jupiter.api.Assertions.assertEquals;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

import com.example.demo.model.Foods;

@SpringBootTest
public class FoodsTest {
	/*
	 * Foods foods = new Foods(10,"Briyani",150,"yes");
	 * 
	 * @Test public void checkId() { assertEquals(foods.getFid(),10); }
	 * 
	 * @Test public void checkName() { assertEquals(foods.getfName(), "Briyani"); }
	 * 
	 * @Test public void checkPrice() { assertEquals(foods.getfPrice(),150); }
	 * 
	 * @Test public void checkAvalilable() {
	 * assertEquals(foods.getAvailable(),"yes"); }
	 * 
	 */
}
